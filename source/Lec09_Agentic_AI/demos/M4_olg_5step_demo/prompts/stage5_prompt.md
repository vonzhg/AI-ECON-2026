# Claude Code Prompt: Stage 5 Implementation

Read all files in `stages/`.

Implement or revise the OLG solver so it follows the approved pseudo-code exactly. After editing, run:

```bash
/usr/bin/python3 run.py solve
/usr/bin/python3 run.py check
PYTHONPATH=src /usr/bin/python3 -m unittest discover -s tests
```

Report the equilibrium interest rate, capital-market gap, borrowing-constraint mass, and which figures were generated.
